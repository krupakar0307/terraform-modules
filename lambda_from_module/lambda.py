def handler(event,context):
    a=1002
    b=1983

    sum = a+b
    sub = b-a
    mul = a*b
    div = a/b

    print(f"Hello, this are my outputs sum = {sum}, sub = {sub}, mul = {mul}, div= {div}" )
